import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akibbagwan07.ghraisonitimtable',
  appName: 'G H Raisoni Timetable',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
